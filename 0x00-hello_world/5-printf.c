#include <stdio.h>

/**
 * main(void) - serves as the entry point for the program
 * printf() - prints the exact string asigned to it and uses a newline character
 */

int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,\n");
  return(0);
}
