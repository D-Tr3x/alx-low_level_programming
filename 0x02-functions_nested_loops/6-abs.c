#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of integer
 * @n: the integer that is checked and/or converted
 *
 * Return: the absolute value of the integer
 */

int _abs(int n)
{
if (n > 0)
return (n);
else
return (-n);
}
